// Generated by CoffeeScript 1.10.0
'use strict';
angular.module('subzapp_mobile').controller('LoginController', [
  '$scope', '$state', '$http', '$window', 'message', 'RESOURCES', '$rootScope', function($scope, $state, $http, $window, message, RESOURCES, $rootScope) {
    console.log('Login Controller');
    $scope.user = {};
    $rootScope.USER = null;
    return $scope.login_submit = function() {
      console.log("loginsss " + (JSON.stringify($scope.login_form_data)));
      $('.login_error').css('display', 'none');
      $http.post(RESOURCES.DOMAIN + "/auth/signin", $scope.login_form_data).success(function(data) {
        window.localStorage.setItem('user_token', data.token);
        window.localStorage.setItem('user_id', data.user.id);
        $state.go('all_org');
      }).error(function(err) {
        console.log(err);
        $('.login_error').show('slide', {
          direction: 'right'
        }, 1000);
        return $state.go('register');
      });
    };
  }
]);
